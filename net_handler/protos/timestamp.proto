/*
 * Defines a message for communicating time stamps
 */

syntax = "proto3";

option optimize_for = LITE_RUNTIME;

message TimeStamps {
    uint64 dawn_timestamp = 1;
    uint64 runtime_timestamp = 2;
}
