/**
 * Defines the type of message being transferred
 * Do not try to compile only this file
 */

syntax = "proto3";

enum Msg {
	GAMEPAD_STATE = 0;
	DEVICE_DATA = 1;
	RUN_MODE = 2;
	CHALLENGE_DATA = 3;
	LOG = 4;
}