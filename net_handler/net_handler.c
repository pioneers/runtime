#include "net_handler.h"

//IP addresses of Dawn and Shepherd and port numbers

int main ()
{
	//set up
	
	while (1) {
		//something something open sockets and wait for connections
		//on connection spawn threads to handle communication
		//handle dropped connections and opening new sockets in this loop too
	}
	
	return 0;
}